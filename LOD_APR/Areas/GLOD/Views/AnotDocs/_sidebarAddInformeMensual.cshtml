@model LOD_APR.Areas.GLOD.ModelsViews.AddInformeView
@using LOD_APR.Areas.GLOD.ModelsViews;

<div class="form-horizontal">
    @Html.AntiForgeryToken()
    <input type="hidden" name="IdAnotacion" value="@Model.IdAnotacion" />
    <input type="hidden" name="IdContrato" value="@Model.IdContrato" />
    @if (Model.IdInforme > 0)
    {
<div class="row">
    <div class="col-sm-12">
        <table class="table table-hover small m-b-xs">
            <tbody>
                <tr>
                    <td style="padding:4px 0;border-top:none">
                        <strong>Folio Interno</strong>
                    </td>
                    <td style="padding:4px 0;border-top:none">
                        @Model.Folio
                    </td>
                </tr>
                <tr>
                    <td style="padding:4px 0;border-top:none">
                        <strong>Creado el</strong>
                    </td>
                    <td style="padding:4px 0;border-top:none">
                        @Model.Fecha
                    </td>
                </tr>
                <tr>
                    <td style="padding:4px 0;border-top:none">
                        <strong>Mes Informado</strong>
                    </td>
                    <td style="padding:4px 0;border-top:none">
                        @Model.MesInformado
                    </td>
                </tr>
                <tr>
                    <td style="padding:4px 0;border-top:none">
                        <strong>Periodo</strong>
                    </td>
                    <td style="padding:4px 0;border-top:none">
                        @Model.Periodo
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

                <select-item-informe :id-informe="@Model.IdInforme"></select-item-informe> }
                            else
                            {
                <h3>El Informe Ejecutivo para este periodo no se encuentra cargado aún.</h3>}



</div>

